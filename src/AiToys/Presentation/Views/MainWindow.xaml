<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="AiToys.Presentation.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:AiToys.Presentation.ViewModels"
    xmlns:views="using:AiToys.Presentation.Views"
    Title="AiToys"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="ShellViewModelTemplate" x:DataType="viewModels:ShellViewModel">
                <views:ShellView DataContext="{Binding}" />
            </DataTemplate>
        </Grid.Resources>

        <ContentControl Content="{x:Bind ViewModel.ShellViewModel}" ContentTemplate="{StaticResource ShellViewModelTemplate}" />
    </Grid>
</Window>
